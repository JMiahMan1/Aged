# Athena contrib port (CTM engine for Chipped)

- **Upstream**: https://github.com/terrarium-earth/Athena (MIT, ThatGravyBoat)
- **Base**: `26.x` branch (closest to our 26.2 target)
- **Status**: sources staged at `custom-mods/athena/src` (common+fabric java,
  `athena.mixins.json`); NOT yet wired into `settings.gradle` — inert until
  the port lands. Next: wire module, compile against 26.2, boot-smoke.
- **Patches**: recorded here under `patches/` when the port lands.
